// utils.js

function getImageUrl(imageName) {
    return `C:\Users\User\new-react-website\public\images/${imageName}`;
  }
  
  // export the function so that it can be used in other files
  export default getImageUrl;
  